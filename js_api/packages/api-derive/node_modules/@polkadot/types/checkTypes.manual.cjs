"use strict";

var _util = require("@polkadot/util");

var _index = require("./create/index.cjs");

// Copyright 2017-2021 @polkadot/types authors & contributors
// SPDX-License-Identifier: Apache-2.0
function registryCheck(registry) {
  // Should be AccountId
  const aa = registry.createType('AccountId');
  (0, _util.assert)(aa.isAscii, 'All ok'); // Should be Codec

  const bb = registry.createType('Something');
  (0, _util.assert)(bb.toHuman(), 'All ok'); // Should be u32

  const cc = registry.createType('Something');
  (0, _util.assert)(cc.divn(123), 'All ok'); // Should be u32

  const dd = registry.createType('Something');
  (0, _util.assert)(dd.divn(123), 'All ok');
}

registryCheck(new _index.TypeRegistry());